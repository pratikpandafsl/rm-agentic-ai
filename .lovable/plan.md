# Agent Configuration

## Build
- Add an authenticated Agent Configuration page matching the supplied reference.
- Include the six-agent selector, central agent flow, and editable configuration panel.
- Make Test and Save version provide clear demo feedback.
- Link Agent Configuration from the sidebar.
- Apply the same page and interactions to the standalone React Router project and refresh its ZIP.

## Technical details
- Keep the live preview’s existing routing conventions.
- Keep the downloadable project React Router-only with no TanStack dependency.
- Reuse the existing visual tokens and shared application layout.
- Verify the page in the browser and confirm the standalone project builds.
