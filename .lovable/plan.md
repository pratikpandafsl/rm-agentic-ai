# Output File

## Build
- Add a protected Output File page matching the supplied classification-output reference.
- Show the account, history, state, root cause, action, confidence, and relied-on fields table.
- Add an Export CSV action that downloads the displayed rows.
- Link Output File in the sidebar and add the matching route.

## Deliverables
- Update the live preview.
- Update and repackage the standalone React Router project without TanStack dependencies.

## Verification
- Confirm the page renders, navigation is active, CSV export works, and both builds pass.
